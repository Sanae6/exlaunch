#include "customPlayer.hpp"

namespace ca {
    BirdPlayer::BirdPlayer(const char* name) : PlayerActorBase(name) {}

    void BirdPlayer::initPlayer(const al::ActorInitInfo&, const PlayerInitInfo&) {
        
    }
}